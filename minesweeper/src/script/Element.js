class Element {
  constructor (element, classes, parentElement, text) {
    this.element = element;
    this.classes = classes;
    this.parentElement = parentElement;
    this.text = text;
  }

  build () {
    
  }
}